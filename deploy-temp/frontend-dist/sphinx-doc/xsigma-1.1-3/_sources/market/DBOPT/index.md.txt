# DBOPT - Rate Deterministic & Inflation Deterministic Models

## 📋 **Vue d'Ensemble**

Le modèle **DBOPT-RateDeterministic_InflationDeterministic** est un framework de pricing déterministe utilisé pour l'évaluation de produits financiers dépendant des taux d'intérêt et de l'inflation sans optionalité. Ce modèle fait partie de la bibliothèque DBAnalytics et constitue une méthodologie avancée pour le pricing de titres ayant une solution analytique fermée.

---

## 🎯 **Caractéristiques Principales**

### **Modèles Couverts**
- **DBOPT-RateDeterministic_InflationDeterministic**
- **DBX-RateDeterministic_InflationDeterministic**

### **Produits Supportés**
- **InflSwapZC*** - Inflation Zero Coupon Swaps
- **InflSwapYOY*** - Inflation Year-on-Year Swaps
- **InflAssetSwap*** - Inflation Asset Swaps
- **IRSwapRealRate*** - Interest Rate Real Rate Swaps
- **IRIndexSingle_INFLATION*** - Single Inflation Index

### **Usages Approuvés**
- ✅ **End of Day (EOD)** - Évaluation et gestion des risques
- ✅ **VaR/HistSim/Multivariate** - Calculs de Value at Risk
- ✅ **CCAR** - Tests de stress réglementaires
- ✅ **IRRBB** - Interest Rate Risk in Banking Book

---

## 🏗️ **Architecture du Framework**

### **Approche Déterministe**
Le framework utilise une approche déterministe où :
- Les **taux d'intérêt** sont des fonctions déterministes du temps
- L'**inflation** suit un modèle déterministe sans volatilité
- Le **pricing** est basé sur des arguments d'arbitrage sans volatilité

### **Composants Techniques**
```cpp
// Exemple d'implémentation de courbe d'inflation dans Our project
class inflation_curve : public market_data {
private:
    datetime base_date_;
    ptr<interpolator<std::vector<double>, datetime, double>> inverse_dfs_;
    std::vector<double> seasonality_adjustment_;
    ptr_const<inflation_fixing> historic_fixings_;

public:
    double cpi(const datetime& to, const tenor& observation_lag) const;
    void initialize();
};
```

---

## 📊 **Évaluation des Risques**

### **Global Model Risk Assessment**

| Critère | Rating | Justification |
|---------|--------|---------------|
| **Complexité** | LOW(1)/LOW(2) | Jugement SME du validateur |
| **Incertitude** | LOW(1)/LOW(2) | Méthodologies sans faiblesses majeures |
| **Dépendance** | HIGH(1)/HIGH(2) | Modèles de pricing à forte dépendance |

---

## 📚 **Documentation Détaillée**

```{toctree}
:maxdepth: 2
:caption: Documentation DBOPT

executive_summary
introduction_model_validation
model_calibration_risk_sensitivities
model_change_description
validation_outline
detailed_model_testing
independent_validation_testing
product_testing_inflassetswap
product_testing_inflswapyoy
```

### **Sections Principales**

#### **📋 Documentation Fondamentale**
- **[Executive Summary](executive_summary.md)** - Résumé exécutif et évaluation globale des risques
- **[Introduction & Model Validation](introduction_model_validation.md)** - Scope, description des produits et méthodologie
- **[Model Calibration & Risk Sensitivities](model_calibration_risk_sensitivities.md)** - Calibration et calcul des sensibilités
- **[Model Change Description](model_change_description.md)** - Gestion des changements et évolutions du modèle
- **[Validation Outline](validation_outline.md)** - Plan de validation et méthodologies de test

#### **🔬 Tests et Validation Détaillés**
- **[Detailed Model Testing](detailed_model_testing.md)** - Tests spécifiques détaillés du modèle (interpolation, délai, saisonnalité)
- **[Independent Validation Testing](independent_validation_testing.md)** - Tests de validation indépendants et frameworks réglementaires

#### **📊 Tests de Produits Spécifiques**
- **[Product Testing: InflAssetSwap](product_testing_inflassetswap.md)** - Tests complets pour Inflation Asset Swaps
- **[Product Testing: InflSwapYOY](product_testing_inflswapyoy.md)** - Tests complets pour Inflation Year-on-Year Swaps

---

## 🔧 **Implémentation Technique Avancée**

### **Courbes d'Inflation avec Ajustements Saisonniers**
Le framework utilise des courbes d'inflation sophistiquées construites à partir d'instruments de marché :

```cpp
// Construction de courbe d'inflation avec ajustements saisonniers dans Our project
double inflation_curve::cpi(const datetime& to, const tenor& observation_lag) const {
    const auto date = datetime_helper::add_tenor(to, observation_lag, -1);

    if (date < base_date_) {
        return historic_fixings_->cpi(date);
    }

    if (XSIGMA_LIKELY(!seasonality_adjustment_.empty())) {
        year_month_day mdy(date);
        return cpi_b_ * inverse_dfs_->interpolate(date) *
               seasonality_adjustment_.at(year_month_day(date).month - 1);
    } else {
        return cpi_b_ * inverse_dfs_->interpolate(date);
    }
}
```
*Cette implémentation C++ dans Our project montre comment la courbe d'inflation calcule les valeurs CPI en tenant compte des ajustements saisonniers mensuels et des fixings historiques pour assurer la continuité temporelle.*

### **Initialisation et Validation**
```cpp
// Initialisation complète de la courbe d'inflation
void inflation_curve::initialize() {
    validate();

    cpi_b_ = historic_fixings_->cpi(base_date_);

    // Enregistrement des paramètres pour différentiation automatique
    AAD_REGISTER_PARAMETER(inverse_dfs_, inverse_dfs_->state_parameters_size());
    AAD_REGISTER_PARAMETER(seasonality_adjustment_, seasonality_adjustment_.size());
    AAD_REGISTER_PARAMETER(cpi_b_, 1);
    AAD_REGISTER_PARAMETER(historic_fixings_, historic_fixings_->state_parameters_size());
}
```
*Cette méthode d'initialisation dans Our project configure tous les paramètres nécessaires pour la différentiation automatique (AAD), permettant un calcul efficace des sensibilités.*

### **Paramètres de Configuration Critiques**

| Paramètre | Valeur par Défaut | Plage Approuvée | Description |
|-----------|-------------------|-----------------|-------------|
| `CURVE::InflationInterpolation` | PWCONST_L | (FLAT, DIR, LINEAR, PWCONST_L, DIR-10, LINEAR-10, FLAT-15, PWCONST_L-15) | Méthode d'interpolation pour extraire les figures d'inflation |
| `CURVE::AddPar` | NONE | (NONE, LINEAR) | Technique d'interpolation pour insérer les taux de swap par implicites |
| `CURVE::CashToFuture` | LINEAR | (EXACT, EXACTCASH, EXACTCASH-1, CASHPRIORITY, LINEAR, NONE) | Technique d'interpolation pour inférer le taux stub des futures |
| `CURVE::ExtrapMethod` | NATIVE | (CONSTANT_FORWARD, CONSTANT_PAR, CONSTANT_ZERO, NATIVE) | Méthode de calcul des taux au-delà de ExtrapTime |

---

## 🎯 **Avantages Techniques du Framework**

### **✅ Performance et Précision**
- **Calculs analytiques** avec solutions fermées pour produits linéaires
- **Précision numérique élevée** (erreurs < 0.01 bp)
- **Performance optimisée** pour calculs batch et temps réel
- **Intégration native** avec DBAnalytics et Our project

### **✅ Robustesse et Validation**
- **Tests de stress complets** sous scénarios CCAR et IRRBB
- **Validation indépendante** avec implémentations alternatives
- **Monitoring continu** des paramètres via MARS
- **Conformité réglementaire** vérifiée

### **✅ Flexibilité d'Implémentation**
- **Support multi-plateforme** (Excel, Python, Perl)
- **Intégration Kannon** et Risk Engine
- **APIs standardisées** pour développement
- **Documentation complète** et exemples d'usage

---

## 📈 **Cas d'Usage Détaillés**

### **Évaluation End-of-Day Avancée**
```cpp
// Exemple d'évaluation EOD pour portefeuille d'inflation
class InflationPortfolioEvaluator {
public:
    struct EODResults {
        double total_pv;
        std::map<std::string, double> product_pv;
        std::map<std::string, double> sensitivities;
        std::map<std::string, double> risk_metrics;
    };

    EODResults evaluate_portfolio(
        const InflationPortfolio& portfolio,
        const MarketData& market_data) {

        EODResults results;

        // Calcul PV par produit
        for (const auto& product : portfolio.get_products()) {
            double pv = product->calculate_pv(market_data);
            results.product_pv[product->get_id()] = pv;
            results.total_pv += pv;
        }

        // Calcul des sensibilités
        results.sensitivities = calculate_portfolio_sensitivities(portfolio, market_data);

        // Métriques de risque
        results.risk_metrics = calculate_risk_metrics(portfolio, market_data);

        return results;
    }
};
```

### **Gestion des Risques Multivariée**
- **Calculs VaR** avec corrélations entre facteurs de risque
- **Tests de stress** sous scénarios réglementaires
- **Analyse de sensibilité** paramétrique complète
- **Monitoring temps réel** des expositions

### **Pricing de Produits Complexes**
- **Swaps d'inflation** zero-coupon et year-on-year
- **Asset swaps** indexés sur inflation avec spreads
- **Produits de taux réels** avec ajustements d'inflation
- **Indices composites** multi-devises

---

## 🔍 **Surveillance et Contrôle Qualité**

### **Model Parameter Monitoring**
- **Surveillance automatisée** via MARS
- **Alertes** sur déviations paramétriques
- **Reporting** périodique des performances
- **Validation continue** des résultats

### **Contrôles de Cohérence**
- **Tests de réplication** quotidiens
- **Validation croisée** avec modèles alternatifs
- **Monitoring des écarts** de pricing
- **Analyse des tendances** temporelles

---

*Cette documentation complète du framework DBOPT intègre les meilleures pratiques de Our project avec une validation rigoureuse et des implémentations techniques avancées pour le pricing déterministe de produits d'inflation.*
